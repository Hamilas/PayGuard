# Local docker-compose requirements — FraudRadar-MLOps
# Slimmed-down dependency set for running the core fraud-scoring pipeline
# locally without AWS, K3s, Airflow, or the agentic layer.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
xgboost==2.0.3
mlflow==2.14.1
prometheus-client==0.20.0
httpx==0.27.0
pyarrow==15.0.2
redis==5.0.6
python-dotenv==1.0.1
