FROM python:3.11-slim

WORKDIR /app

COPY requirements/requirements-local.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY paystream/paystream.py /app/paystream.py

EXPOSE 8001

CMD ["uvicorn", "paystream:app", "--host", "0.0.0.0", "--port", "8001"]
